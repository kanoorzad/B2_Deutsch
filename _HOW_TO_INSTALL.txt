B2_Deutsch — FULL PACKAGE (app Build v41 + Persian audio Piper workflow)
========================================================================
This zip mirrors your repository layout. Copy files into your repo keeping the
SAME folders (overwrite existing).

WHAT'S IN HERE
--------------
APP (repo root):
  index.html ............ Build v41 — modernised styling pass 1:
                          • Samim font for all Persian/Arabic text + UI
                          • larger, more legible translation text
                          • refined dark theme tokens
                          (all audio/voice/playback logic unchanged)
  service-worker.js ..... v41 cache
  data.js, integration.js, manifest.webmanifest, icon.svg

PERSIAN AUDIO (free, no Azure):
  generate_audio_piper.py ................ Piper TTS generator
  .github/workflows/generate-fa-audio-piper.yml .. "Build Persian audio pack (FREE - Piper)"
  (other language workflows + azure generator included as-is)

INSTALL
-------
1) Extract, copy everything into your repo keeping folders, overwrite, commit.
2) Hard-refresh the app once (service worker is sticky) to load Build v41.

NOTES
-----
• The visual redesign is in progress — this is Pass 1 (fonts, sizing, colours).
  The compact nav top bar + book/unit/language picker sheets come in later passes.
• Persian audio: if any words are still silent, re-run the Piper workflow; it
  skips existing clips and fills only the gaps.
