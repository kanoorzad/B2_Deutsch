name: Build Deutsch audio (Azure Neural TTS)

# Generate audio with Azure Neural TTS and commit the MP3s into ./audio.
# Run manually from the Actions tab: click "Run workflow", then pick which
# language pack to build (or "all"). Re-runs skip clips that already exist,
# so building one language at a time is the recommended way to stay under
# each job's time limit.

on:
  workflow_dispatch:
    inputs:
      language:
        description: 'Which language pack to generate'
        required: true
        default: 'de'
        type: choice
        options:
          - de
          - en
          - es
          - ru
          - uk
          - tr
          - ar
          - fa
          - all

permissions:
  contents: write

jobs:
  build-audio:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Pick jobs file
        id: pick
        run: |
          if [ "${{ github.event.inputs.language }}" = "all" ]; then
            echo "jobs=audio_jobs.csv" >> "$GITHUB_OUTPUT"
          else
            echo "jobs=audio_jobs_${{ github.event.inputs.language }}.csv" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate audio
        env:
          AZURE_SPEECH_KEY: ${{ secrets.AZURE_SPEECH_KEY }}
          AZURE_SPEECH_REGION: ${{ secrets.AZURE_SPEECH_REGION }}
          JOBS_CSV: ${{ steps.pick.outputs.jobs }}
        run: |
          python generate_audio_azure.py

      - name: Commit new audio
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add audio
          if git diff --cached --quiet; then
            echo "No new audio to commit."
          else
            git commit -m "Add/update ${{ github.event.inputs.language }} audio clips"
            git push
          fi
